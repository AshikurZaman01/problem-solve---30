// write an arrow function that will take 2 parameters: one parameters will come from you and the other parameters will be a default parameter. Add these two parameters and return the result.
//

const arroe = (num1 , num2 = 2) => num1 * num2;

console.log(arroe(4))